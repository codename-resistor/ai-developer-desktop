---
layout: feature
name: "Moby Engine"
status: "stable"
---

Moby Engine provides the Docker container runtime for workflows that depend on Docker specifically. It runs alongside Podman using its own daemon and socket.

## Technical Details

Includes `moby-engine-rootless-extras` for running Docker in rootless mode.
